**Chapter 4 exercise 2**

> 다음 표를 완성하여라.

|선언 및 초기화|
|---|
|int a = 1, b = 2, c = 3;|
|double x = 1.0;|

|수식|동일한 수식|값|
|---|---|:---:|
|a > b && c < b |**(b <= a) && (b >= c)**|**0**|
|a < ! b \|\| ! ! a|**(a >= b) \|\| a**|**1**|
|a + b < ! c + c|**(a + b) < ((!c) + c)**|**0**|
|a - x \|\| b * c && b / a|**(a - x) \|\| (b * c) && (b / a)**|**1**|